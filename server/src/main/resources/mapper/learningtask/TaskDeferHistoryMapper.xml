<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.learningtask.infrastructure.persistence.TaskDeferHistoryMapper">
    <insert id="insert">
        INSERT INTO learn_task_defer_history (
            id, assignment_id, source_task_id, target_task_id,
            source_scheduled_date, target_scheduled_date, defer_type,
            operator_user_id, occurred_at
        ) VALUES (
            #{history.id}, #{history.assignmentId}, #{history.sourceTaskId}, #{history.targetTaskId},
            #{history.sourceScheduledDate}, #{history.targetScheduledDate}, #{history.deferType},
            #{history.operatorUserId, jdbcType=BIGINT}, #{history.occurredAt}
        )
    </insert>
</mapper>
