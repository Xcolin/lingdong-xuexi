<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.learningtask.infrastructure.persistence.ReviewerTransferMapper">
    <insert id="insert">
        INSERT INTO learn_task_reviewer_transfer (
            id, assignment_id, from_reviewer_user_id, to_reviewer_user_id,
            transferred_by_user_id, transfer_reason, transferred_at
        ) VALUES (
            #{transfer.id}, #{transfer.assignmentId}, #{transfer.fromReviewerUserId},
            #{transfer.toReviewerUserId}, #{transfer.transferredByUserId},
            #{transfer.transferReason}, #{transfer.transferredAt}
        )
    </insert>
</mapper>
