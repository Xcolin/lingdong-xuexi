<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.learningtask.infrastructure.persistence.TaskAssignmentEventMapper">
    <insert id="insert">
        INSERT INTO learn_task_assignment_event (
            id, assignment_id, event_type, operator_user_id, from_status,
            to_status, reason, event_details, occurred_at
        ) VALUES (
            #{event.id}, #{event.assignmentId}, #{event.eventType}, #{event.operatorUserId},
            #{event.fromStatus}, #{event.toStatus}, #{event.reason, jdbcType=VARCHAR},
            #{event.eventDetails, jdbcType=VARCHAR}, #{event.occurredAt}
        )
    </insert>
</mapper>
