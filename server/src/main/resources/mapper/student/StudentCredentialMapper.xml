<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.student.infrastructure.persistence.StudentCredentialMapper">
    <resultMap id="StudentCredentialResultMap" type="com.lingdong.learning.student.domain.StudentCredential">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="student_user_id" javaType="java.lang.Long"/>
            <arg column="code_hash" javaType="java.lang.String"/>
            <arg column="code_salt" javaType="java.lang.String"/>
            <arg column="key_version" javaType="java.lang.String"/>
            <arg column="failure_count" javaType="java.lang.Integer"/>
            <arg column="captcha_required" javaType="java.lang.Boolean"/>
            <arg column="locked_until" javaType="java.time.LocalDateTime"/>
            <arg column="code_updated_at" javaType="java.time.LocalDateTime"/>
            <arg column="last_success_at" javaType="java.time.LocalDateTime"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <select id="findByStudentUserId" resultMap="StudentCredentialResultMap">
        SELECT id, student_user_id, code_hash, code_salt, key_version, failure_count, captcha_required,
               locked_until, code_updated_at, last_success_at, created_at, updated_at
        FROM auth_student_credential
        WHERE student_user_id = #{studentUserId}
    </select>

    <select id="findByStudentUserIdForUpdate" resultMap="StudentCredentialResultMap">
        SELECT id, student_user_id, code_hash, code_salt, key_version, failure_count, captcha_required,
               locked_until, code_updated_at, last_success_at, created_at, updated_at
        FROM auth_student_credential
        WHERE student_user_id = #{studentUserId}
        FOR UPDATE
    </select>

    <insert id="insert">
        INSERT INTO auth_student_credential (
            id, student_user_id, code_hash, code_salt, key_version, failure_count, captcha_required
        ) VALUES (
            #{credential.id}, #{credential.studentUserId}, #{credential.codeHash}, #{credential.codeSalt},
            #{credential.keyVersion}, #{credential.failureCount}, #{credential.captchaRequired}
        )
    </insert>

    <update id="resetLoginCode">
        UPDATE auth_student_credential
        SET code_hash = #{digest.hash},
            code_salt = #{digest.salt},
            key_version = #{digest.keyVersion},
            failure_count = 0,
            captcha_required = 0,
            locked_until = NULL,
            code_updated_at = CURRENT_TIMESTAMP,
            updated_at = CURRENT_TIMESTAMP
        WHERE student_user_id = #{studentUserId}
    </update>

    <update id="updateFailureState">
        UPDATE auth_student_credential
        SET failure_count = #{failureCount},
            captcha_required = #{captchaRequired},
            locked_until = #{lockedUntil, jdbcType=TIMESTAMP},
            updated_at = CURRENT_TIMESTAMP
        WHERE student_user_id = #{studentUserId}
    </update>

    <update id="markLoginSuccess">
        UPDATE auth_student_credential
        SET failure_count = 0,
            captcha_required = 0,
            locked_until = NULL,
            last_success_at = #{succeededAt},
            updated_at = CURRENT_TIMESTAMP
        WHERE student_user_id = #{studentUserId}
    </update>
</mapper>
