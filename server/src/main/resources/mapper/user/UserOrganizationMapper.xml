<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.user.infrastructure.persistence.UserOrganizationMapper">

    <select id="exists" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM sys_user_organization
            WHERE user_id = #{userId}
              AND organization_id = #{organizationId}
        )
    </select>

    <insert id="insert">
        INSERT INTO sys_user_organization (id, user_id, organization_id)
        VALUES (#{id}, #{userId}, #{organizationId})
    </insert>
    <select id="findOrganizationIds" resultType="long">
        SELECT organization_id FROM sys_user_organization WHERE user_id = #{userId}
    </select>
</mapper>
