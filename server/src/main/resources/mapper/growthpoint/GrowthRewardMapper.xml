<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.growthpoint.infrastructure.persistence.GrowthRewardMapper">
    <resultMap id="GrowthRewardMap" type="com.lingdong.learning.growthpoint.domain.GrowthReward">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="student_id" javaType="java.lang.Long"/>
            <arg column="created_by_parent_id" javaType="java.lang.Long"/>
            <arg column="reward_name" javaType="java.lang.String"/>
            <arg column="required_points" javaType="java.lang.Long"/>
            <arg column="description" javaType="java.lang.String"/>
            <arg column="expires_at" javaType="java.time.LocalDateTime"/>
            <arg column="status" javaType="com.lingdong.learning.growthpoint.domain.GrowthRewardStatus"/>
            <arg column="version_no" javaType="java.lang.Integer"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
            <arg column="deleted_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <sql id="RewardColumns">
        id, student_id, created_by_parent_id, reward_name, required_points,
        description, expires_at, status, version_no, created_at, updated_at, deleted_at
    </sql>

    <insert id="insert">
        INSERT INTO growth_reward (
            id, student_id, created_by_parent_id, reward_name, required_points,
            description, expires_at, status, version_no, created_at, updated_at, deleted_at
        ) VALUES (
            #{reward.id}, #{reward.studentId}, #{reward.createdByParentId},
            #{reward.rewardName}, #{reward.requiredPoints}, #{reward.description},
            #{reward.expiresAt}, #{reward.status}, #{reward.versionNo},
            #{reward.createdAt}, #{reward.updatedAt}, #{reward.deletedAt}
        )
    </insert>

    <select id="findManagedByStudentId" resultMap="GrowthRewardMap">
        SELECT <include refid="RewardColumns"/>
        FROM growth_reward
        WHERE student_id = #{studentId} AND status &lt;&gt; 'DELETED'
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countManagedByStudentId" resultType="long">
        SELECT COUNT(*)
        FROM growth_reward
        WHERE student_id = #{studentId} AND status &lt;&gt; 'DELETED'
    </select>

    <select id="findAvailableByStudentId" resultMap="GrowthRewardMap">
        SELECT <include refid="RewardColumns"/>
        FROM growth_reward
        WHERE student_id = #{studentId}
          AND status = 'ONLINE'
          AND (expires_at IS NULL OR expires_at &gt; #{now})
        ORDER BY required_points, created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAvailableByStudentId" resultType="long">
        SELECT COUNT(*)
        FROM growth_reward
        WHERE student_id = #{studentId}
          AND status = 'ONLINE'
          AND (expires_at IS NULL OR expires_at &gt; #{now})
    </select>

    <select id="findByIdForUpdate" resultMap="GrowthRewardMap">
        SELECT <include refid="RewardColumns"/>
        FROM growth_reward
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <update id="update">
        UPDATE growth_reward
        SET reward_name = #{reward.rewardName},
            required_points = #{reward.requiredPoints},
            description = #{reward.description},
            expires_at = #{reward.expiresAt},
            status = #{reward.status},
            version_no = version_no + 1,
            updated_at = #{reward.updatedAt}
        WHERE id = #{reward.id}
          AND status &lt;&gt; 'DELETED'
          AND version_no = #{expectedVersion}
    </update>

    <update id="softDelete">
        UPDATE growth_reward
        SET status = 'DELETED',
            deleted_at = #{deletedAt},
            updated_at = #{deletedAt},
            version_no = version_no + 1
        WHERE id = #{id}
          AND status &lt;&gt; 'DELETED'
          AND version_no = #{expectedVersion}
    </update>

    <select id="findExpiredOnlineIds" resultType="long">
        SELECT id
        FROM growth_reward
        WHERE status = 'ONLINE'
          AND expires_at IS NOT NULL
          AND expires_at &lt;= #{now}
        ORDER BY expires_at, id
        LIMIT #{limit}
    </select>

    <update id="offlineExpired">
        UPDATE growth_reward
        SET status = 'OFFLINE',
            version_no = version_no + 1,
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND status = 'ONLINE'
          AND expires_at IS NOT NULL
          AND expires_at &lt;= #{updatedAt}
    </update>
</mapper>
