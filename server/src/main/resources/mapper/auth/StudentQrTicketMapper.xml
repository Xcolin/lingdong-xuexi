<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingdong.learning.auth.infrastructure.persistence.StudentQrTicketMapper">
    <resultMap id="StudentQrTicketResultMap" type="com.lingdong.learning.auth.domain.StudentQrTicket">
        <constructor>
            <idArg column="id" javaType="java.lang.Long"/>
            <arg column="student_id" javaType="java.lang.Long"/>
            <arg column="student_user_id" javaType="java.lang.Long"/>
            <arg column="token_hash" javaType="java.lang.String"/>
            <arg column="status" javaType="com.lingdong.learning.auth.domain.StudentQrTicketStatus"/>
            <arg column="expires_at" javaType="java.time.LocalDateTime"/>
            <arg column="consumed_at" javaType="java.time.LocalDateTime"/>
            <arg column="issued_by_user_id" javaType="java.lang.Long"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <update id="revokeActiveByStudentId">
        UPDATE auth_student_qr_ticket
        SET status = 'REVOKED', updated_at = CURRENT_TIMESTAMP
        WHERE student_id = #{studentId} AND status = 'ACTIVE'
    </update>

    <insert id="insert">
        INSERT INTO auth_student_qr_ticket (
            id, student_id, student_user_id, token_hash, status, expires_at, consumed_at, issued_by_user_id
        ) VALUES (
            #{ticket.id}, #{ticket.studentId}, #{ticket.studentUserId}, #{ticket.tokenHash},
            #{ticket.status}, #{ticket.expiresAt}, #{ticket.consumedAt, jdbcType=TIMESTAMP},
            #{ticket.issuedByUserId}
        )
    </insert>

    <select id="findByTokenHashForUpdate" resultMap="StudentQrTicketResultMap">
        SELECT id, student_id, student_user_id, token_hash, status, expires_at, consumed_at,
               issued_by_user_id, created_at, updated_at
        FROM auth_student_qr_ticket
        WHERE token_hash = #{tokenHash}
        FOR UPDATE
    </select>

    <select id="findByTokenHash" resultMap="StudentQrTicketResultMap">
        SELECT id, student_id, student_user_id, token_hash, status, expires_at, consumed_at,
               issued_by_user_id, created_at, updated_at
        FROM auth_student_qr_ticket
        WHERE token_hash = #{tokenHash}
    </select>

    <update id="markConsumed">
        UPDATE auth_student_qr_ticket
        SET status = 'CONSUMED', consumed_at = #{consumedAt}, updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND status = 'ACTIVE'
    </update>
</mapper>
