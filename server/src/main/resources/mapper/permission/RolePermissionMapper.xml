<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="com.lingdong.learning.permission.infrastructure.persistence.RolePermissionMapper"><select id="exists" resultType="boolean">SELECT EXISTS(SELECT 1 FROM sys_role_permission WHERE role_id=#{roleId} AND permission_id=#{permissionId})</select><insert id="insert">INSERT INTO sys_role_permission(role_id,permission_id) VALUES(#{roleId},#{permissionId})</insert></mapper>
