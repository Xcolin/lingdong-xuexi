package com.lingdong.learning.interfaceconfig.domain;

/** Controls whether a registered interface service is permitted to record calls. */
public enum InterfaceServiceStatus {
    ENABLED,
    DISABLED
}
