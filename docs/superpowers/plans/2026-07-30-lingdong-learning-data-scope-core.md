# 灵动学习数据权限范围核心实施计划

**Goal:** 计算用户对组织树节点的有效数据访问范围，落实角色数据范围、用户组织关联和组织管理员范围的交集规则。

**Rules:** `ALL`角色作为系统全局范围直接放行；普通角色需以角色分配组织或自定义范围为根，并同时命中用户组织关联。用户存在组织管理员绑定时，目标还必须落在任一管理员组织子树内。`SELF`不授权组织树数据，供后续个人/学生实体判断使用。

### Tasks

- [x] 编写集成测试：全局管理员可访问，教师需命中角色范围和用户组织范围，组织管理员范围进一步收敛。
- [x] V9 创建角色自定义组织范围关系；复用已有组织管理员关系表。
- [x] 实现角色自定义范围、组织管理员配置和组织数据范围判定服务。
- [x] 运行专测和完整后端回归。
