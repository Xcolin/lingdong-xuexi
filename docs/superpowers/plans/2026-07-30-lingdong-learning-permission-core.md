# 灵动学习功能权限核心实施计划

**Goal:** 建立权限目录、角色权限、用户补充与禁止权限，以及可按权限编码计算有效访问结果的后端核心。

**Precedence:** 本实现覆盖功能权限层：功能开关由调用方先行校验；用户`DENY`优先于角色授权和用户`ALLOW`；任一有效角色授权或用户`ALLOW`可放行。数据范围、组织范围的交集作为下一项独立能力。

### Tasks

- [x] 写测试：角色授权可放行，用户拒绝可覆盖角色授权，用户补充允许可放行无角色用户。
- [x] V8 创建用户权限表，保存`ALLOW`或`DENY`效果并确保用户权限唯一。
- [x] 实现权限目录、角色授权、用户权限配置和有效权限判定服务。
- [x] 运行专测和完整后端回归。
