# 灵动学习 V31 任务图片打卡与统一附件访问实施计划

> **执行要求：** 按任务使用 TDD 先观察失败再写最小实现；每完成一项更新复选框。只使用本地 H2 和受控测试目录，不连接远程 MySQL、Redis、微信或对象存储。

## 任务一：V31 迁移与规则基线

- [x] 先扩展迁移测试，断言文件模块/分类/摘要字段、任务图片规则、扩展名、权限和 19 位基础标识。
- [x] 确认 V31 不存在时测试失败。
- [x] 新增 `V31__add_task_checkin_attachment.sql`，历史文件安全回填后设置非空约束。
- [x] 验证 V1-V31 连续迁移、55 张非自增主键表和 V31 敏感信息扫描。

## 任务二：统一内容存储与上传 API

- [x] 先写失败测试覆盖 JPG/PNG 上传、伪造文件头、超限、错误客户端、元数据隐藏和未关联删除。
- [x] 增加文件内容存储端口、本地受控实现、上传编排、访问服务、Controller 和响应。
- [x] 扩展文件领域与 Mapper，固化模块、分类、摘要和删除状态。
- [x] 验证路径穿越防护、内容响应头和所有标识字符串化。

## 任务三：打卡附件原子关联与查询

- [x] 端到端测试覆盖纯图片、图文混合、空提交、越权读取和已关联文件删除冲突；原子性由同一事务边界保障。
- [x] 扩展打卡请求命令，允许可空文字与最多 9 个文件标识。
- [x] 在任务实例事务中校验并关联附件，保持打卡、状态、事件和文件关系原子一致。
- [x] 学生任务与审核详情查询最近打卡附件，越权内容读取返回 404。

## 任务四：uni-app 图片打卡

- [x] 扩展可测试的附件请求契约并纳入类型检查。
- [x] 使用 `uni.chooseImage` 和 `uni.uploadFile` 实现选图、进度、失败重试、移除和预览。
- [x] 提交 `content + fileIds`，成功后清空文字与附件；会话失效由统一认证请求处理。
- [x] 运行类型检查、H5 和微信小程序构建。

## 任务五：Web 审核附件预览

- [x] 先写组件失败测试，断言附件元数据和认证内容读取。
- [x] 扩展认证 Blob 请求和审核类型，在抽屉展示图片缩略信息与预览，卸载时释放对象 URL。
- [x] 运行 Web 目标测试、全量测试和生产构建。

## 任务六：回归、文档与进度

- [x] 执行后端全量测试，记录 V1-V31、161 项测试和 55 表结果。
- [x] 执行 Web 与小程序全量验证、`git diff --check`、主键、自增、敏感信息和前端产物扫描。
- [x] 同步 00-07、09、10、12、README 和主开发计划，明确 V31 只开放 JPG/PNG 图片任务打卡。
- [x] 按固定 1000 分口径更新项目进度，并自动进入任务逾期/顺延设计。
