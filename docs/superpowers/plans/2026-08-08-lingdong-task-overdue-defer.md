# 灵动学习 V32 任务待优化与顺延实施计划

> **执行要求：** 按任务使用 TDD 先观察失败再写最小实现；每完成一项更新复选框。只使用本地 H2，不连接远程 MySQL、Redis、微信、共享测试、预生产或生产环境。

## 任务一：V32 迁移与模型基线

- [x] 扩展 Flyway 失败测试，断言任务生成类型、实例顺延字段、历史表、事件约束、权限和 19 位标识。
- [x] 新增 `V32__add_task_overdue_defer.sql`，不修改 V1-V31。
- [x] 验证 V1-V32 连续迁移、56 张显式非自增主键表，并完成 V32 新增范围敏感信息扫描。

## 任务二：23:59 待优化处理

- [x] 先写服务测试覆盖进行中到待优化、有效暂停排除、非目标状态、重复处理和功能停用。
- [x] 增加到期实例游标查询、单实例事务服务、批处理服务和上海时区 23:59:59 调度。
- [x] 写入 `MARKED_NEEDS_IMPROVEMENT` 系统事件，不产生积分或负面记录。

## 任务三：自动与手动顺延

- [x] 先写服务测试覆盖自动顺延、手动 1/7/8 天边界、手动优先、自动后手动和已认领冲突。
- [x] 实现任务定义与标签复制、实例重置、历史追加和隔夜标识。
- [x] 增加 00:00 自动批处理及实例级独立事务，保证重复与并发幂等。
- [x] 增加 Web 管理端目录与顺延 API、动态权限、来源对象范围和中性错误响应。

## 任务四：Web 与 uni-app

- [x] Web 在独立待优化任务视图提供顺延日期操作，覆盖权限、状态、提交反馈和刷新。
- [x] uni-app 任务列表与详情展示隔夜迁移标签，不增加学生顺延入口。
- [x] 运行 Web 目标测试/构建及小程序类型检查、H5 和微信小程序构建。

## 任务五：回归、文档与进度

- [x] 执行后端 175 项全量测试；主键、自增、敏感信息、产物和 `git diff --check` 在最终收口检查执行。
- [x] 同步 00-07、09、10、12、README 和总计划，只把实际完成范围标记为本地完成。
- [x] 按固定 1000 分口径更新进度，并自动进入“复制昨日任务”设计。
