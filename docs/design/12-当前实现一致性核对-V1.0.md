# 灵动学习当前实现一致性核对

**版本**：V1.0（V22 实现快照）

**核对日期**：2026-08-01

**状态**：已核对，非完整项目验收结论

**核对依据**：[设计追溯](00-设计文档体系与需求追溯-V1.0.md)、`server/`、`web/`、`miniapp/` 实际代码及 Flyway `V1` 至 `V22`

## 1. 核对结论

当前实现已形成组织与 RBAC、系统任务审核、功能开关、数据字典、缓存、接口服务、附件、模板元数据、平台账号 Web 会话、首批 IAM/组织/用户管理、学生关系、机构家长邀请，以及学生 8 位账号加 4 位登录码的小程序登录基础。Web 与 uni-app 是两个独立前端工程；V22 在 V21 学生身份与登录链路之上，新增学生当前班级、教师班级关系，以及家庭、机构、教师三类学习任务的候选项、草稿、编辑、发布、批量独立发布和学生本人只读查询。

当前仍不是完整可上线的灵动学习系统。短信与微信登录、二维码登录、副家长和关系变更、消息投递、任务认领、暂停、打卡、审核转交、积分奖励、复盘、导入导出执行、统计报表、对象存储及真实第三方适配尚未完成。因此只能标记为**V1-V22 当前基础能力已实现并通过本地自动化回归**，不能标记为 BRD 全功能验收完成。

## 2. 已实现且已回归的能力

| 设计项 | 实现证据 | 当前状态与限制 |
|---|---|---|
| 组织树、组织类型与组织管理员 | V2-V4、V17；`organization`、`datascope` 模块和 Web 组织页 | 首批查询、新增和管理员配置已实现；全量树仍仅系统管理员可用，编辑、移动、删除及组织范围 SQL 通用化未完成。 |
| 用户、角色与动态权限 | V2、V4、V8、V9、V16、V18；IAM/User Controller 和 Web 页面 | 自定义角色、角色权限、用户 `ALLOW/DENY`、组织关联、自定义数据范围、用户目录和状态管理已实现；用户级 `DENY` 覆盖角色允许。 |
| 系统任务与审核员边界 | V5；`SystemTaskApplicationService` | 仅系统管理员提交、系统审核员审批且禁止自审；普通业务审核不授予系统审核员。通用任务载荷执行器仍未完整覆盖所有配置模块。 |
| 功能开关 | V6、V7、V21-V22；`FeatureAccessService` | 全局开关变更可审批；定位能力默认关闭。`STUDENT_CODE_LOGIN` 和 `LEARNING_TASK_MANAGEMENT` 均接入公开能力摘要、前端入口、直达页和后端接口拦截。 |
| 字典、缓存、接口、附件与模板核心 | V10-V14 及对应模块 | 核心服务、约束和操作台账已实现；REST/OpenAPI、对象存储、预览下载、导入导出执行及真实适配器仍未完成。 |
| 平台账号 Web 认证 | V15；`auth` 模块、Spring Security Bearer 链 | BCrypt 密码、访问/刷新令牌摘要、轮换、设备下线和状态撤销已实现；仅 `PLATFORM` 用户可密码登录并创建 `WEB` 会话。 |
| 学生关系与机构家长邀请 | V19-V20；`student` 模块 | 家长或直接机构管理员创建学生，按亲子/组织对象范围查询；机构可向既有家长账号签发一次性邀请，数据库只存 SHA-256 摘要。副家长、消息投递和关系变更未实现。 |
| 学生账号与登录凭证 | V21；账号序列、凭证表、身份发放和凭证管理服务 | 新学生同事务创建独立 `STUDENT` 用户、角色、`YYNNNNNN` 账号和 4 位登录码；历史学生可由主家长或直接机构管理员初始化。明文只在创建、初始化或重置成功响应出现一次。 |
| 学生登录安全 | V21；HMAC 凭证、验证码、限流、锁定与会话服务 | 独立随机盐、HMAC-SHA256 和密钥版本已实现；第 5 次失败要求验证码，第 10 次锁定 15 分钟；验证码一次消费并绑定账号设备，保护存储不可用时失败关闭。 |
| 学生小程序登录 | V21；`StudentAuthenticationController`、公开能力接口、uni-app 登录与主页 | 学生只创建 `MINIAPP` 会话，Web 密码登录拒绝学生账号；首页按开关隐藏入口，直达登录页重新核验开关，登录码与验证码答案不持久化。扫码和微信登录未实现。 |
| 学生与教师班级关系 | V22；`StudentClassAssignmentService`、`TeacherClassAssignmentService` | 机构管理员可在授权组织子树内配置学生唯一活动班级和教师活动班级；教师只读取本人活动班级，关系停用后保留历史。 |
| 学习任务定义与发布 | V22；`learningtask` 模块 | 家长、机构管理员、教师按各自对象和组织范围创建、编辑、查询和发布任务；发布时重新校验范围并展开学生，重叠目标去重，实例初始状态为 `PENDING_CLAIM`。批量发布逐项使用独立事务并返回中性失败结果。 |
| 学习任务 Web 与小程序 | V22；Web 任务管理页、uni-app 任务列表和详情 | Web 提供筛选、草稿编辑、单项和批量发布及班级配置；小程序仅允许学生读取本人任务列表与详情，不提前提供认领、打卡或审核操作。功能停用后入口隐藏、直达阻断、后端拒绝。 |
| Flyway 与雪花主键 | V1-V22；`IdGenerator`、`SnowflakeIdGenerator` | 当前 38 张表均使用应用层生成的 19 位雪花 `BIGINT id` 主键；V22 新增的任务、目标、标签、实例和教师班级关系均不使用数据库自增。 |

## 3. 与设计基线的未实现差距

| 优先级 | 差距 | 影响与进入顺序 |
|---|---|---|
| P0 | 任务认领、暂停、打卡、审核转交、积分、奖励和复盘 | V22 已完成任务定义、发布和学生只读查询，但尚未形成执行到激励的完整学习闭环；后续按学生执行、业务审核、积分奖励、复盘顺序进入。 |
| P0 | 组织数据范围 SQL 通用化与功能开关统一拦截 | V22 已在学生对象、学生登录和学习任务中落地，但后续新增业务接口仍需逐项按“开关、权限、数据范围、状态”接入。 |
| P0 | 短信/微信/二维码认证及完整家校关系 | 当前学生仅支持账号登录码；消息投递、副家长、解绑、转班转学和注销仍缺失。 |
| P1 | 配置模块 REST/OpenAPI 与缓存处理器 | 字典、缓存、接口、附件和模板已有核心服务，但管理端契约、权限/组织/功能/统计缓存处理器未完成。 |
| P1 | 文件、导入导出、消息、统计与第三方适配 | 尚不能形成完整上传预览、模板执行、业务通知、复杂统计和受控外部调用闭环。 |
| P1 | 地图与未成年人位置能力 | 需求已正确默认关闭且当前小程序产物不含位置页面；具备主体资质、审批和授权条件后再专项实施。 |

## 4. V21-V22 关键边界

1. 学生账号按当前年份后两位加 6 位序号生成，每年从 `000001` 递增到 `999999`；数据库行锁、期望值更新和唯一约束共同控制并发，账号不循环复用。
2. 创建学生、创建学生用户、授予 `STUDENT` 角色、写入凭证和建立家长或机构关系处于同一事务，任一步失败均不留下半成品身份。
3. 历史学生初始化和重置同时要求 RBAC 权限与对象范围。无关家长和跨机构管理员统一得到 404，避免泄露学生是否存在；重复初始化和非法状态返回 409。
4. 登录码使用测试与生产分离的版本化密钥。生产配置只保留环境变量占位，启动时要求当前版本 Base64 密钥解码后不少于 32 字节；代码、迁移和前端产物不保存真实密钥。
5. 未知账号、账号类型错误、账号或学生停用、凭证缺失和登录码错误统一返回 `STUDENT_AUTH_FAILED`，并执行等价摘要比较；错误响应不区分账号是否存在。
6. 登录按账号设备和来源地址限流，验证码按来源限流。生产使用 Redis 原子脚本，测试使用独立内存实现；Redis 异常返回 `AUTH_PROTECTION_UNAVAILABLE`，不降级绕过安全控制。
7. 登录码重置覆盖摘要和随机盐，并在同一事务中撤销该学生全部活动会话；客户端退出即使服务端请求失败也会清除本地会话。
8. 小程序 H5 仅用于独立调试与兼容构建，开发代理指向本地 `127.0.0.1:8080`；微信小程序产物仍由 `build:mp-weixin` 独立生成，Web 端状态和路由不与小程序复用。
9. 微信、数据库和 Redis 的真实连接参数不进入本次测试。当前测试只使用本地 H2、测试专用密钥、测试验证码和内存保护存储。
10. 家庭任务只能面向当前家长活动主关系学生；机构任务只能使用机构管理员授权组织子树；教师任务只能使用教师本人活动班级及其学生。跨范围任务统一按不存在处理，避免对象枚举。
11. 任务创建、编辑和发布均由后端重新计算来源、目标、审核人与基础积分，不信任前端提交的权限范围或派生值；只有 `DRAFT` 可编辑和发布。
12. 发布事务先锁定草稿，再重新校验范围、展开并去重学生、批量写入实例，最后更新任务状态；没有有效学生时整单拒绝。批量发布通过独立 Bean 的 `REQUIRES_NEW` 逐项隔离，单项失败不回滚其他成功项。
13. 学生任务查询从当前 `MINIAPP` 会话解析学生身份，不接受请求方指定学生标识；Web 管理端与小程序端不复用路由或会话类型。

## 5. 本次回归证据

### 5.1 后端

执行目录：`server/`

执行环境：JDK 17

执行命令：`mvn test`

结果：**Tests run: 112, Failures: 0, Errors: 0, Skipped: 0；BUILD SUCCESS。** Flyway 已在本地 H2 MySQL 兼容模式成功校验并从空库连续执行 22 个迁移到 V22。V22 新增覆盖包括学生班级切换、教师班级关系、受范围保护的候选项、三类任务草稿和发布、目标展开去重、批量独立发布、学生本人任务查询、公开能力接口及功能停用。

### 5.2 Web

执行目录：`web/`

执行命令：`npx vitest run --pool=forks --maxWorkers=1 --minWorkers=1`、`npm run build`

结果：**5 个测试文件、11 项测试通过；`tsc --noEmit` 与 Vite 生产构建成功。** 覆盖任务来源按角色显示、草稿发布、功能关闭后的入口和路由判断，以及既有用户、组织和接口行为。`1280×800` 本地 Vite 视觉检查覆盖任务列表、编辑抽屉和发布确认；检查中发现并修复了宽表撑开页面的问题，复验时页面宽度为 1280px、无页面级横向溢出，1050px 宽表仅在面板内部滚动。功能停用后菜单隐藏，直达任务路由返回工作台。构建仅保留公共分包超过 500 KB 的提示。

### 5.3 小程序

执行目录：`miniapp/`

执行命令：`npm run type-check`、`npm run build:h5`、`npm run build:mp-weixin`

结果：**类型检查、H5 构建和微信小程序构建均成功。** V22 已增加按来源筛选的学生本人任务列表和只读详情，并在首页和直达页检查学习任务功能开关。`390×844` H5 视觉检查覆盖首页入口、三来源列表、家庭来源筛选和详情；页面无横向溢出，家庭筛选后仅保留家庭任务，详情不含认领、打卡或审核按钮。功能停用后详情直达页只显示不可用状态且不渲染任务详情。构建输出的 `os - Alias not found` 与 Sass Legacy JS API 弃用提示来自当前 uni-app 工具链，不影响产物生成。

### 5.4 环境限制

本次没有访问共享测试环境，没有在预生产或生产数据库执行，也没有连接用户提供的远程 MySQL、Redis 或微信服务。视觉检查使用本地 Vite 服务与符合当前 API 契约的虚拟 Mock 数据，不作为真实接口联调证据。正式上线前仍须在经批准的 MySQL 8、Redis 和微信开发环境完成迁移、并发、故障、安全与业务 UAT；本地 H2 和 Mock 结果不能替代这些验证。

## 6. 后续实施准入

1. 后续数据库变更从 V23 顺序新增，不修改 V1-V22；每张新表继续使用应用层 19 位雪花 `BIGINT` 主键。
2. 新业务 Controller 必须同时接入认证、客户端类型、功能开关、RBAC、对象/组织数据范围和业务状态校验，不创建临时绕过接口。
3. 任务执行、业务审核与积分模块进入前，先同步 FSD、数据库、API、安全和测试文档，并以 V22 任务定义、实例和学生身份模型作为唯一数据来源。
4. 真实密钥、数据库密码、Redis 密码和微信密钥只通过本地忽略配置或环境变量注入，不写入 Git、Flyway、日志、文档或前端产物。
