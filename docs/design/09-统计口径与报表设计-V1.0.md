# 灵动学习统计口径与报表设计

**版本**：V1.0（设计基线草案）  
**状态**：待评审  
**业务依据**：BRD 第 4 章；原始素材中的复盘生成与完成率规则仅在不冲突处补充  
**关联设计**：[FSD](01-功能详细设计-FSD-V1.0.md)、[数据库设计](04-数据库设计-V1.0.md)、[API 设计](06-API接口设计-V1.0.md)

## 1. 统计通用原则

1. 统计仅从用户有权访问的数据集计算，不得先聚合全量数据再通过前端隐藏。
2. 复杂统计、全量台账、复杂筛选和导出以 Web 为主；小程序只显示与当前角色相关的摘要和待办。
3. 所有时间范围按中国标准时间计算；日统计按自然日、周统计按自然周、月统计按自然月。明确生成时点的复盘按每日 21:00、每周一 00:00、每月 1 日 00:00 生成。
4. 功能关闭后不生成新的受控功能统计、复盘或导出；历史数据是否可读遵循 FSD 和具体功能开关规则。
5. 所有导出记录导出人、时间、筛选条件、范围、原因和文件；敏感导出须先有系统任务审批，并执行脱敏。

## 2. 指标口径

### 2.1 已明确指标

| 指标 | 公式/定义 | 数据来源 | 维度与筛选 | 展示角色 |
|---|---|---|---|---|
| 区域数、学校数、机构数 | 统计状态为启用的对应组织节点数；停用节点不计入当前数 | `sys_organization` | 区域、组织类型、状态 | 系统管理员 |
| 用户数 | 授权范围内用户数量，按用户状态和角色可筛选 | `sys_user`、`sys_user_role`、`sys_user_organization` | 组织、角色、状态、时间 | 系统管理员/授权管理员 |
| 待审系统任务数 | `sys_system_task.status = 待审核` 的数量 | `sys_system_task` | 任务类型、状态、时间、影响范围 | 系统审核员、系统管理员 |
| 任务完成数 | 统计期间任务实例状态为“已完成”的数量 | `learn_task_assignment` | 学生、来源、组织、班级、时间 | 家长、教师、机构管理员、学生本人 |
| 任务完成率 | `已完成任务实例数 / (统计期间任务实例总数 - 进行中任务实例数)`；原始素材明确“进行中不计入分母” | `learn_task_assignment` | 学生、任务来源、班级、组织、日期范围 | 家长、教师、机构管理员、学生本人 |
| 当日待优化数 | 统计日内状态转为“待优化”的任务实例数 | `learn_task_assignment`、`learn_task_adjustment` | 学生、来源、组织、日期 | 家长、教师、机构管理员、学生本人 |
| 暂停次数 | 统计期间情绪暂停或难题搁置记录数，按暂停类型可分组 | `learn_task_pause` | 学生、班级、类型、日期 | 家长、教师、机构管理员、学生本人 |
| 累计总积分 | 学生积分账户的当前累计积分；任务奖励增加，V26 整笔纠错减少，兑换不得减少 | `growth_point_account` | 学生、日期快照 | 家长、学生 |
| 可用积分 | 学生积分账户当前可用积分 | `growth_point_account` | 学生、日期快照 | 家长、学生 |
| 累计获取积分 | 统计期间 `TASK_REWARD` 正向积分减去关联的 `CORRECTION` 负向积分；兑换和休眠清零不计入 | `growth_point_ledger` | 学生、来源任务、来源组织、时间 | 家长、教师、机构管理员（仅机构公开来源）、学生 |
| 兑换待办数 | 当前状态为“兑换待审批”的兑换申请数 | `growth_reward_exchange` | 学生、状态、申请时间 | 主家长、学生本人 |
| 考勤状态统计 | 按签到、签退、迟到、早退、缺勤、请假状态分组计数 | `attendance_record` | 学校、班级、学生、日期 | 机构管理员、教师、家长仅本人、学生本人 |
| 异常报备数 | 统计期间异常报备记录数，按异常类型、状态分组 | `edu_exception_report` | 班级、学生、类型、状态、时间 | 教师、机构管理员 |

### 2.2 V27 兑换统计口径

1. 学生提交申请时不变更积分账户，因此不能按申请数推算已消费积分；兑换消费只统计状态已进入 `PENDING_VERIFICATION` 或 `VERIFIED` 且存在唯一 `REDEMPTION` 台账的数据。
2. 兑换台账固定 `amount=0`、`available_delta=-required_points_snapshot`。累计总积分不因兑换减少，可用积分在主家长同意时减少；驳回、自动驳回和到期均不产生扣分台账。
3. 奖励名称、所需积分和描述以兑换快照为历史口径，不能通过关联当前奖励值回算；当前奖励被编辑、下架或逻辑删除不改变历史报表。
4. 待办数只统计 `PENDING_APPROVAL`；待核销数只统计 `PENDING_VERIFICATION`；已核销数只统计 `VERIFIED`。按状态分组时不得把 `REJECTED`、`AUTO_REJECTED`、`EXPIRED` 合并为已消费。
5. 家庭奖励和兑换只对主家长及学生本人展示，教师和机构统计不得通过统一积分账户或学生标识反推、汇总或导出家庭私有奖励。

### 2.3 V29 积分生命周期口径

1. 固定任务连续天数按同一任务标识、学生、来源类型、来源主体和相邻计划自然日计算；同标题的新任务或复制任务不合并连续天数。
2. 第 1 至 7 天实发比例为 100%，第 8 至 15 天为 80%，第 16 天起为 60%；台账同时保留基础积分、实发积分、衰减比例、连续天数和命中规则标识。
3. 纠错后的原奖励从连续历史中排除；纠错后重新审核形成的新奖励按其计划日期参与计算，统计报表以不可变台账实发值为准。
4. 沉睡天数从任务有效操作、非休眠清零积分变动和用户主动复盘补录三类事实的最后发生时点计算。登录、查看、自动生成复盘、提醒创建和清零自身不计活跃。
5. 第 30 天清零只减少可用积分，累计总积分不变；`DORMANCY_CLEAR` 不计入累计获取积分，也不作为下一沉睡周期的活跃事实。
6. 第 27 天提醒记录按学生和沉睡周期去重。`PENDING` 只代表待消息系统投递，`NO_RECIPIENT` 代表当时无活动主家长，两者都不能统计为送达。
7. V30 每日固定计划只负责生成任务实例；统计继续以实例计划日和最终状态为事实。停止计划不删除已生成实例，未生成的未来日期不得补记为任务总数或未完成数。
8. V31 图片附件不新增任务完成事实。打卡次数按 `learn_task_checkin` 提交记录计数，不按附件数量计数；图片大小、数量和格式只进入附件管理台账，不改变任务完成率、连续完成或积分口径。
9. V32 顺延迁移同一任务实例的 `scheduled_date`，不在来源日期继续计入任务总数、待优化数或完成率分母；迁移后从目标日期事实集中统计。顺延不产生积分、完成或打卡事实。
10. V33 复制成功后以新任务和新实例计入目标日期统计，不回写昨日统计，不继承来源任务完成、打卡、暂停、顺延或积分事实；复制批次成功数和失败数属于过程审计，不计作学生任务完成数。
11. V34 模板本身不是任务事实，不进入任务总数、完成率、积分、复盘或排行。只有选用模板后通过现有流程创建并发布的新任务，才按其计划日期和目标学生进入统计。
12. 已生成的日/周/月复盘快照保持不可变。顺延造成历史期间事实变化时，后续受控重算以新版本快照反映，并保留旧版本，不直接改写历史快照。

### 2.4 需要业务确认后固化的指标

| 编号 | 指标 | 当前缺口 | 实施要求 |
|---|---|---|---|
| R-001 | 免执行任务是否计入完成率分母 | 原始素材只明确“进行中不计入分母”，未明确免执行处理 | 确认后统一修订任务完成率、待优化占比、班级完成率和验收用例。 |
| R-002 | 学员活跃度 | BRD 要求展示活跃度趋势，但未给出“活跃”的行为集合与阈值 | 明确任务认领、打卡、积分变动、登录等是否计入及最小次数。 |
| R-003 | 成长趋势曲线的数值 | BRD 要求趋势，但未定义单一得分或展示多个原始指标 | 明确是完成率、积分、待优化、暂停次数的多折线，还是综合指标。 |
| R-004 | 机构任务完成率的任务范围 | 未明确是否包含机构下发后未被教师再次下发的任务，以及跨班级转移处理 | 明确任务实例纳入条件和时间归属。 |

在 R-001 至 R-004 未确认前，界面可以展示已明确的原始指标，但不得将推测公式标记为最终业务口径。

## 3. 首页与角色看板

| 看板对象 | 卡片与图表 | 数据范围 | 刷新规则 |
|---|---|---|---|
| 系统管理员 | 区域、学校、机构、用户数；功能开关状态；待审系统任务；趋势/列表 | 全局且按审计需要 | 实时/日，复杂统计 Web 完整展示 |
| 系统审核员 | 待审核、已审核、驳回系统任务 | 仅系统任务 | 实时 |
| 机构管理员 | 班级数、学员数、机构任务完成率、考勤统计、活跃度趋势 | 授权机构/学校树 | 日/周/月；Web 完整，小程序摘要 |
| 教师 | 本班任务完成率、学生考勤状态、异常报备数 | 授权班级 | 实时/日 |
| 家长 | 当日完成率、待审核数、累计/可用积分、待优化任务数 | 已绑定学生；家庭和机构结果分区 | 实时/日 |
| 学生 | 当日任务、累计/可用积分、成长徽章、复盘入口 | 本人 | 实时，小程序主展示 |

## 4. 报表与台账

| 报表/台账 | 主要字段 | 筛选 | 导出 | 权限与脱敏 |
|---|---|---|---|---|
| 系统任务审批台账 | 任务类型、发起人、审批人、状态、时间、意见 | 时间、状态、类型 | Excel | 系统管理员/审核员各自授权范围。 |
| 权限变更日志 | 操作人、角色、权限项、前后值、时间 | 时间、角色、操作人 | Excel | 系统管理员；敏感字段脱敏。 |
| 附件管理台账 | 名称、所属模块、上传人、时间、类型、大小、状态 | 模块、上传人、时间、类型 | Excel | 只显示有业务权限的附件元数据。 |
| 缓存操作日志 | 缓存类型、模块、操作、操作人、时间、结果 | 时间、模块、结果 | Excel | 系统管理员。 |
| 数据字典台账 | 类型、编码、名称、排序、状态、更新时间 | 类型、状态 | Excel | 系统管理员。 |
| 导入导出模板台账 | 名称、类型、适用模块、版本、状态、更新时间 | 类型、模块、状态 | Excel | 系统管理员。 |
| 接口服务台账 | 名称、用途、调用方、授权范围、状态、责任人 | 调用方、状态、责任人 | Excel | 系统管理员；不导出密钥。 |
| 学生任务报表 | 学生、任务、来源、状态、积分、审核状态 | 学生、来源、状态、时间 | Excel/PDF | 家长限绑定学生；机构/教师限机构公开范围。 |
| 家庭复盘报告 | 完成率、积分、暂停次数、待优化数、趋势 | 学生、日期范围 | PDF | 家庭私有，按亲子关系；机构不可访问。 |
| 积分明细报表 | 获取时间、来源任务、数量、类型、审核状态 | 学生、时间、任务类型 | Excel | 来源和组织范围双重校验。 |
| 奖励兑换报表 | 奖励、所需积分、申请时间、审批、核销状态 | 学生、状态、时间 | Excel | 主家长；机构和教师不可访问。 |
| 机构任务统计 | 学校、班级、任务数、完成数、完成率、平均积分 | 区域、学校、班级、时间 | Excel/PDF | 仅授权机构范围，家庭数据排除。 |
| 考勤统计报表 | 学生、签到、签退、迟到、早退、缺勤、请假 | 学校、班级、日期 | Excel/PDF | 开关、授权、组织范围均满足。 |
| 异常报备台账 | 学生、异常类型、报备教师、状态、时间 | 班级、类型、状态 | Excel | 教师/机构限授权范围。 |

## 5. 成长复盘数据集

### 5.1 日复盘

| 项目 | 规则 |
|---|---|
| 生成 | 每日 21:00 自动生成当日初稿；受每日复盘功能开关控制。 |
| 指标 | 当日任务完成率、积分获取总量、情绪暂停次数、待优化任务数；进行中任务不计任务完成率分母。 |
| 补录 | 家长/学生可补充成长感悟、优缺点总结、次日改进计划；仅当天及次日内，补录标记需保存。 |
| 关闭状态 | 停止新复盘生成，保留历史复盘查询。 |

V28 固化口径如下：完成率分母为“任务总数减进行中数量”，分子为已完成数量，分母为 0 时完成率为 0；累计获取积分只合计期间内 `TASK_REWARD` 与 `CORRECTION` 的 `amount`，`REDEMPTION` 和未来 `DORMANCY_CLEAR` 不计入获取积分。任务分类来自任务快照分类，暂停次数来自期间内暂停事实。免执行是否纳入分母仍遵循 R-001，当前以任务总数中的既有状态事实展示，不推导新的完成口径。

### 5.2 周/月复盘

| 项目 | 规则 |
|---|---|
| 生成 | 每周一 00:00 生成上周；每月 1 日 00:00 生成上月。 |
| 指标 | 任务完成率、累计获取积分、任务类型分布、待优化任务占比、情绪暂停频次、成长趋势曲线。 |
| 模板 | 简洁版展示核心数据；详细版包含数据明细、成长分析和改进建议。 |
| 导出 | 支持单日、单周、单月和指定时间段；PDF 功能关闭后隐藏新导出入口，历史文件按权限可查看。 |

V28 已实现自然周、自然月快照和每日趋势数据，但“简洁/详细模板”、指定时间段报告、PDF 文件生成和订阅投递尚未实现。每次生成对统计事实计算 SHA-256 指纹：指纹不变时复用当前版本，迟到事实导致指纹变化时新增连续版本并更新逻辑复盘的当前快照指针，历史版本保持不可变。

## 6. 数据刷新与性能边界

1. 待办、任务状态、积分余额、消息状态等需实时读取的指标从业务事实表或受控缓存读取。
2. 日/周/月复盘、趋势、匿名排行、机构汇总等可通过定时生成或快照加速；快照须带生成时间和统计范围，不能掩盖数据延迟。
3. 大数据量导出使用导出任务，不在同步请求中生成长时间文件；完成后通知用户下载。
4. 每个报表接口必须限制时间范围、分页/聚合粒度和数据范围，防止通过超大筛选或跨组织聚合规避权限。

## 7. 统计验收清单

- [ ] 指标分子、分母、状态集合、时间边界、数据范围和排除条件均有明确来源。
- [ ] 进行中任务不计入复盘任务完成率分母；免执行处理按 R-001 确认结果统一。
- [ ] 家庭报告、奖励和家庭积分不会被机构或教师报表统计或导出。
- [ ] 机构统计不会跨越授权组织树、班级或停用范围。
- [ ] 导出字段、筛选和脱敏与角色范围一致，敏感导出必须存在审批记录。
- [ ] Web 完整统计与小程序摘要使用同一后端口径，不出现数值不一致。
